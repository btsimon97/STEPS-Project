<% provide(:title, 'Sign up') %>
<% if resource_name.to_s == 'user' %>
  <h1>Teacher Sign Up</h1>
  <p>
    This is the sign-up page for new teachers at participating instituions
    to create a new account. New admin accounts for the institution can be created
    by contacting the STEPS Prject team.
  </p>
  <p>
    In order for registration to be successful, your institution must be registered
    with The STEPS Project. To register your institution with the project, please
    contact the STEPS Project team.
  </p>
  <p>
    Hillsborough County Public Schools Employees should enter their Lawson number
    into the User Id field.
  </p>
<% end %>

<% if resource_name.to_s == "student" %>
  <h1>Student Sign Up</h1>
<% end %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>
      <div class="form-group field">
        <%= f.label :first_name %>
        <%= f.text_field :first_name , autofocus: true,  class: 'form-control' %>
      </div>

      <div class="form-group field">
        <%= f.label :last_name %>
        <%= f.text_field :last_name, class: 'form-control' %>
      </div>

      <div class="form-group field">
        <% if resource_name.to_s == 'user' %>
          <%= f.label :user_number, "User ID #" %>
          <%= f.text_field :user_number, class: 'form-control' %>
        <% end %>
        <% if resource_name.to_s == 'student' %>
          <%= f.label :student_number, "Student ID #" %>
          <%= f.text_field :student_number, class: 'form-control' %>
        <% end %>
      </div>

      <div class="form-group field">
        <%= f.label :email %>
        <%= f.email_field :email, class: 'form-control' %>
      </div>

      <div class="form-group field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %>
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group field">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
      </div>
      <div class="actions">
        <%= f.submit "Sign up", class: "btn btn-primary" %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>
